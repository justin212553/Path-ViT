# 세션 인계 노트 (2026-08-08)

다른 컴퓨터/새 세션에서 이 파일을 읽고 이어서 작업하면 됨. 아래 코드 변경은 전부 git에
커밋·푸시 완료(`origin/main`) — `git pull`만 하면 파일은 이미 다 있음. 이 노트는 "왜 이렇게
했는지"와 "지금 뭘 해야 하는지"를 이어주는 용도.

## 배경

PDAC(췌장암) WSI 생존예측 프로젝트. TCGA-PAAD(152 case) + CPTAC-PDAC(144 case) 두 코호트로
`--dataset tcga --external`(tcga 학습 → cptac을 external test) 방식이 표준 관례. 레퍼런스 리포
(github.com/Leeyoungsup/pancreatic_cancer_pathology)도 같은 Cox partial likelihood loss +
Harrell's C-index/HR/log-rank로 검증 완료(2026-08-08, Method.md/README.md 직접 fetch해서 대조).

이번 세션 모델: `PMA_uni2_INT1500_SS_AUX_STG_R_DISP_COX_ADD`
(`--PMA --backbone uni2 --rna-genes literature_1500_intersection --combine-mode cox_add
--clinical-margin --clinical-staging --patch-keep-frac 0.8 --attn-dispersion --rna-aux-weight 1.0`)

## 이번 세션에서 완료한 것

1. **UNI2-h(ViT-H/14) backbone 파이프라인을 train.py에 연결**
   - `data/dataset.py::FEATURES_FILENAME_BY_BACKBONE`에 `"uni2"` 추가
   - `models/vit_m1.py::TILE_ENCODER_REGISTRY`에 `"uni2": UNI2hEncoder` 추가
   - `train.py --backbone` choices에 `"uni2"` 추가
   - (UNI2hEncoder 자체, `utils/extract_features.py`의 uni2 지원은 이전 세션에 HPC에서 이미 만들어져 있었음)

2. **`sbatch/pma_uni2_coxadd_stg_kfold_array_hpc.sh` 수정** — 원래 `--image`(매 epoch raw jpg
   재디코딩+backbone 재forward, 결과는 같은데 훨씬 느림) 모드로 돌고 있던 걸 precomputed
   `features_uni2.pt`를 직접 읽도록 고침(`--image`/`--tile-decode-workers`/`--cache-*-tiles` 제거).

3. **internal↔external 역전 현상 진단** (tcga→cptac 5-fold 결과: internal pooled c=0.619
   vs external mean c=0.655, 방향이 반대로 나옴 — "도메인 효과라면 internal이 더 높아야
   하는데 왜 낮은가"라는 질문에서 출발):
   - fold별 raw internal은 노이즈가 매우 큼(std=0.086, N~30/fold) vs external은 안정적(std=0.004, N=144 고정)
   - `--dataset cptac --external`(역방향)로 재현 시도 → **부호가 반대로 뒤집힘**(internal
     0.661 > external 0.582) — "internal이 항상 낮다"는 게 구조적 효과가 아니라는 증거
   - **근본 원인으로 확인된 것**: `data/os_labels_{tcga,cptac}.csv` 직접 집계 →
     **TCGA event rate 54.1%(censored 45.9%) vs CPTAC event rate 80.5%(censored 19.5%)**.
     censoring이 많은 코호트를 평가하면 c-index/log-rank의 comparable pair가 줄어 구조적으로
     더 노이지·저평가됨(둘 다 학습에 안 쓰여도 마찬가지) — "어느 쪽이 internal/external이냐"보다
     "어느 코호트를 평가하느냐"가 지배적 변수.
   - IPCW/time-dependent AUC로 censoring 차이를 "보정"하는 안도 검토했으나, **문헌 조사 결과
     주류 관례(Uno et al./scikit-survival)는 train 기준 Ĝ(t) 고정이고, 코호트별로 Ĝ(t)를 바꾸는
     건 표준이 아님**(WebSearch로 확인) — **결론: 데이터를 건드리지 않고(censored 제거/리밸런싱
     금지) 있는 그대로 두고, censoring rate 차이를 명시적으로 병기해서 해석하는 게 맞는 방향.**
     레퍼런스 리포도 이 차이를 전혀 다루지 않음(확인 완료) — 우리가 뒤처진 게 아니라 오히려
     IPCW-AUC까지 추가로 계산하고 있어 더 나아간 상태.

4. **seed 초기화 불안정성 — SAM은 헛수고, 앙상블이 정답**: SAM은 "이미 도착한 basin을 평평하게"
   만들 뿐 "어느 basin에 떨어지는가"는 못 바꿈(사용자가 이미 SAM 시도 후 효과 없음 확인) →
   **예측 평균 앙상블**(여러 독립 학습의 risk score를 평균, weight 평균 아님)을 대안으로 제시.

5. **multi-seed pooling/ensembling 인프라 구축**(3개 신규 파일 + train.py 플래그 1개):
   - `scripts/pool_multiseed_kfold_preds.py` — 3seed(42/84/126) × 5-fold internal 예측을 모아
     (a) seed별 pooled c-index/HR/p, (b) seed 간 분산, (c) **환자 단위 held-out 예측 평균 앙상블**
     (같은 seed의 5-fold checkpoint를 그냥 평균하면 누출 발생 — 그래서 seed마다 fold 배정이
     달라지는 걸 이용해 "그 환자를 한 번도 학습 안 쓴 모델"의 예측만 seed 간 평균), (d) bootstrap 95% CI.
   - `sbatch/pma_uni2_coxadd_stg_multiseed_kfold_array_hpc.sh` — 3seed×5fold=15개 array job으로
     internal 학습(이미 실행 완료, 결과는 아래 참조).
   - `train.py --eval-external-ckpt <경로>` 신규 플래그 — 학습 없이 checkpoint만 불러와 external
     평가 1회 후 환자별 예측을 `.logs/external_preds/`에 CSV 저장하고 종료.
     (external은 어느 seed/fold checkpoint든 그 코호트를 학습에 쓴 적이 전혀 없어서 — internal과
     달리 15개 checkpoint 전부 자유롭게 평균해도 누출 없음, 더 단순함)
   - `sbatch/pma_uni2_coxadd_stg_multiseed_external_eval_hpc.sh` — 위 15개 checkpoint에 대해
     `--eval-external-ckpt`를 순차 호출(재학습 아님, GPU 1개로 몇 분 안에 끝남). **glob 패턴
     버그를 방금 고침**(`*_FOLD..OF.._*_best_pma.pt` → `*FOLD..OF..*best_pma.pt`, 언더스코어
     개수 착각 — 아직 재검증 전, 최신 커밋에 반영됨).
   - `scripts/pool_multiseed_external_preds.py` — 15개 external CSV를 모아 실행별 분산 + 전체
     환자 단위 예측 평균 앙상블 + bootstrap CI.

## 지금까지 나온 실제 숫자 (internal, 3-seed 완료)

```
seed=42:  N=152, events=83 | c_index=0.5984 | HR=1.595 [1.026,2.478] | log_rank_p=0.0362
seed=84:  N=152, events=83 | c_index=0.6456 | HR=1.863 [1.196,2.902] | log_rank_p=0.0052
seed=126: N=152, events=83 | c_index=0.6193 | HR=1.487 [0.955,2.314] | log_rank_p=0.0769
seed 간 pooled c-index: mean=0.6211, std=0.0193

=== 3-seed 예측 평균 앙상블 (N=152, events=83) ===
c_index=0.6359 | HR=1.727 [1.110,2.688] | log_rank_p=0.0142
bootstrap 95% CI(n=2000) = [0.5670, 0.7037], std=0.0347
```

**이 앙상블 값(0.6359)을 internal 헤드라인으로 쓰기로 잠정 결론** — 단, (a) "3-seed 앙상블"임을
명시할 것, (b) 비교 대상 모델(baseline)에도 동일하게 3-seed 앙상블을 적용해야 공정한 비교.

## 다음 세션에서 바로 해야 할 일 (미완료)

1. HPC에서 checkpoint 파일명이 예상과 맞는지 확인:
   ```bash
   ls models/checkpoint/ | grep seed42
   ```
   `survival_tcga_uni2_seed42_..._FOLD0OF5_best_pma.pt` 형태로 15개(seed42/84/126 × fold0~4)
   보이는지 확인.
2. external eval 제출(glob 버그 방금 고침, 아직 실제 HPC에서 검증 안 됨):
   ```bash
   sbatch sbatch/pma_uni2_coxadd_stg_multiseed_external_eval_hpc.sh
   ```
   진행 확인: `squeue -u $USER`, `tail -f .logs/pma_uni2_coxadd_stg_multiseed_external_eval.log`
3. 끝나면 `.logs/external_preds/`에 CSV 15개 있는지 확인 후:
   ```bash
   pip install lifelines   # 아직 그 환경에 없으면(HR/log-rank가 nan으로 나오면 이게 원인)
   python scripts/pool_multiseed_external_preds.py --dataset cptac \
       --model PMA_uni2_INT1500_SS_AUX_STG_R_DISP_COX_ADD --seeds 42,84,126 --n-folds 5 --bootstrap 2000
   ```
4. internal 앙상블(0.6359) vs external 앙상블 결과를 나란히 놓고 비교 — 이번엔 양쪽 다
   3-seed 앙상블이라 지난번(단일 fold/seed) 비교보다 훨씬 공정한 비교가 됨. censoring rate
   차이(TCGA 54% vs CPTAC 80.5%)를 여전히 해석에 병기할 것.
5. (참고, 아직 실행 안 함) `findings_backlog.md`에 이 세션의 핵심 발견(censoring rate가
   internal/external 역전의 진짜 원인, 3-seed 앙상블 프로토콜)을 정식으로 기록할지는 사용자
   판단 필요 — 형식이 이 프로젝트의 기존 로그 스타일과 맞아야 함.

## 참고 — 이번 세션에서 다룬 다른 논의(코드 변경 없음, 맥락만)

- TCGA-BRCA(1058 case) 스케일 검증 실험이 이미 `findings_backlog.md` 최상위에 기록돼 있음 —
  표본을 7배 늘리면 WSI가 순증분 기여를 한다는 걸 이미 확인(2026-07-22). "다른 암종 테스트"
  아이디어는 이미 한 번 해본 것 — 필요하면 `scripts/train_brca_m4.py`에 `--fold`를 적용해
  N=1058 기준 internal std를 PAAD의 std=0.086과 직접 대조해볼 수 있음(미착수).
- 다른 공개 PDAC 코호트(WSI+RNA+생존 3-modality)는 사실상 TCGA+CPTAC뿐 — ICGC/APGI(RNA+임상은
  있으나 공개 WSI 없는 걸로 추정, 미확인)가 유일한 후보.
